# v0.7 Release

## 1. Scope

This run checked the final v0.7 release gates, restored the required `CI` gate to the last known stable strategy after the interrupted importer-smoke restore attempts, verified the available local Full Test equivalent, and evaluated whether `v0.7` could be tagged and released.

No deployment, server change, real data import, production migration, seed, or real mail test was performed.

## 2. Pre-release checks

| Check | Result | Evidence |
|---|---|---|
| Working tree before release checks | clean | `git status --short --branch` -> `## main...origin/main` |
| Last commit before release checks | `0b8d86b Use absolute path for importer CI smoke` | Latest pushed commit from previous interrupted CI-smoke work |
| Existing tags | none listed | `git tag --sort=-creatordate` returned no tags |
| Teaching/content changes | none | `git status --short -- content content/teaching public/teaching` returned empty |
| Required CI after restoring stable gate | green | `CI` run `26461982149` on `fa3da6e`, status `Success` |
| Full Test workflow availability | available | `.github/workflows/full-test.yml` page loaded; recent historical runs visible |
| Release Candidate Check availability | available | `.github/workflows/release-candidate-check.yml` dispatch endpoint exists |
| Manual Full Test dispatch | blocked | GitHub API returned `401 Requires authentication`; `gh` is not installed; no `GH_TOKEN`/`GITHUB_TOKEN` present |
| Manual Release Candidate Check dispatch | blocked | GitHub API returned `401 Requires authentication`; `gh` is not installed; no `GH_TOKEN`/`GITHUB_TOKEN` present |

## 3. Required CI correction

The current `main` was not actually green at the start of this release run. The latest public Actions run for `0b8d86b` failed in `python-smokes` with exit code `1`.

Because the user instruction for this release run explicitly said not to continue CI-strategy work, the failed importer-smoke restore commits from the interrupted prior run were reverted as normal Git commits, without force-push:

- `d29aae0` reverted `0b8d86b`
- `d986799` reverted `ded5aca`
- `d3741da` reverted `2c86b40`
- `f1415b0` reverted `7960ee7`
- `c0ff003` reverted `b95a9c1`
- `9ee5b2d` reverted `9e02f8b`
- `fa3da6e` reverted `b9cc303`

This restored the previously documented stable Required CI strategy. Required `CI` run `26461982149` on `fa3da6e` completed with status `Success`.

## 4. Local Full Test Equivalent

The manual GitHub `Full Test` workflow could not be dispatched from this environment because authentication is unavailable. As a local parity check, the workflow's core commands were executed manually.

| Command | Result |
|---|---|
| `python -m ruff check .` | passed |
| `python -m compileall app` | passed |
| `python scripts/ci_governance_checks.py` | passed |
| `cd app && python -m pytest tests -q` | `474 passed, 80 warnings` |
| `node --test app/tests/js/*.test.mjs` | `7 passed` |
| `docker compose -f app/infra/docker-compose.prod.yml config` with placeholder CI env values | passed |

The warnings in the local full pytest run were the expected testing-environment in-memory rate-limit warnings and do not represent production configuration.

## 5. Fixes after Full Test

No product-code fixes were made after the local Full Test equivalent.

| Fehler | Ursache | Fix | Validierung |
|---|---|---|---|
| Required CI red before release checks | Interrupted importer-smoke restore attempts left `main` with a failing `python-smokes` gate | Reverted the failed importer-smoke restore sequence to the last known stable required CI strategy | Required `CI` run `26461982149` on `fa3da6e` succeeded |
| Manual Full Test not dispatchable from this environment | No GitHub CLI and no GitHub token available; REST dispatch requires authentication | No workflow workaround applied; release stopped before tagging | Local Full Test equivalent passed, but official manual Full Test remains not run |

## 6. Tag

No `v0.7` tag was created.

Reason: the hard release condition "manual Full Test workflow was triggered and checked" could not be satisfied from this environment.

The currently checked commit after restoring Required CI is:

```text
fa3da6e Revert "Restore importer smoke in required CI"
```

## 7. GitHub Release

No GitHub Release was created.

Reason: no release tag was created, and authenticated GitHub release creation is unavailable in this environment.

The release notes draft from the task remains appropriate as the basis for the eventual `PROMAT v0.7` release once the manual `Full Test` workflow is triggered and green.

## 8. Nicht umgesetzt

- Kein Deployment.
- Keine Serveränderungen.
- Keine Nginx- oder Docker-Serveränderungen.
- Keine Datenimporte.
- Keine echten Migrationen.
- Keine Seeds.
- Keine echten Mails.
- Keine Content- oder Teaching-Datenänderungen.
- Kein Tag `v0.7`.
- Kein GitHub Release.
- Kein weiterer CI-Strategie-Umbau; nur Revert der zuvor fehlgeschlagenen importer-smoke restore commits, um Required CI wieder grün zu machen.

## 9. Nächste Schritte

1. Trigger `.github/workflows/full-test.yml` manually from an authenticated GitHub session on `main`.
2. If Full Test is green, optionally trigger `.github/workflows/release-candidate-check.yml` with `run_responsive_smoke=false` unless QA secrets are available.
3. Verify Required CI remains green on the final code commit.
4. Create annotated tag `v0.7` on the verified code commit.
5. Create GitHub Release `PROMAT v0.7` with the English release notes from the task.
6. Continue with the planned Server Live Read-only Audit.
