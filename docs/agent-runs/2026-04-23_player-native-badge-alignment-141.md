# 2026-04-23 Player Native Badge Alignment

- Added the missing `Native Speaker` speaker-group badge to productive player metadata cards for native sessions, so the native card header now mirrors the badge rhythm of learner cards instead of showing only the native-reference badge.
- Kept the player shell neutral and left the canonical native-reference badge in place; the change affects only the badge list in the player metadata card.
- Updated the active player spec and focused player-session regressions to assert the native compare card now renders both `Native Speaker` and the localized native-reference badge.